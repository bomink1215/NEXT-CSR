name: gatchi_sapsida
description: 1인 가구를 위한 초근거리 거래·소통 플랫폼 "같이삽시다"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  geolocator: ^13.0.2        # GPS 위치 가져오기
  geocoding: ^3.0.0          # 위도/경도 → 동네명 변환

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
